import styled from 'styled-components';
const Style = styled.div`
    color: yellow;
    display: flex;
    opacity: 1;
`;
const code = `const foo = 'foo';
const bar = 'bar';
console.log(foo + bar);
`

export default () => {

    return (
        <Style>
            AAA
        </Style>
    )
}